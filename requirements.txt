# Core dependencies
numpy>=1.24.0
pandas>=2.0.0

# Geospatial libraries
geopandas>=0.13.0
rasterio>=1.3.0
shapely>=2.0.0
pyproj>=3.5.0

# Climate data processing
xarray>=2023.1.0
netCDF4>=1.6.0
cdsapi>=0.6.1

# HTTP requests
requests>=2.31.0

# Logging (built-in, but specify for clarity)
# No need to install, part of Python standard library
